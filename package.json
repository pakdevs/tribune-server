{
  "name": "news-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "lint": "eslint api --ext .js",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "test": "tsx --test tests/*.mjs"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.20.5"
  }
}

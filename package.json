{
  "name": "news-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "lint": "eslint api --ext .js",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "test": "tsx --test tests/*.mjs",
    "smoke": "node scripts/smoke.mjs",
    "warmup": "node scripts/ping-warmup.mjs",
    "seed:allowlist": "node scripts/seed-allowlist.mjs"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.4",
    "@vercel/kv": "^3.0.0"
  }
}
